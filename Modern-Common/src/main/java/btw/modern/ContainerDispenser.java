package btw.modern;
public class ContainerDispenser extends Container {
    public ContainerDispenser(IInventory playerInv, IInventory dispenser) {}
    public boolean canInteractWith(EntityPlayer player) { return true; }
}
