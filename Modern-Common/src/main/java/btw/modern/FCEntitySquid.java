package btw.modern;

/** Type-only stub — see FCEntityCow. Referenced by vanilla Entity and EntityLiving. */
public class FCEntitySquid extends EntityWaterMob {
    public FCEntitySquid(World world) { super(world); }
    @Override public int getMaxHealth() { return 10; }
}
