package btw.modern;

/** Type-only stub — see FCEntityCow. Referenced by vanilla EntityLiving. */
public class FCEntityCreeper extends EntityCreeper {
    public FCEntityCreeper(World world) { super(world); }
}
