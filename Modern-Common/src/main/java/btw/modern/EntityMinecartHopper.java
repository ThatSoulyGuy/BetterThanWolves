package btw.modern;

/**
 * Type-only stub. EntityPlayer.displayGUIHopperMinecart takes this type,
 * so it must exist for Modern-Common to compile. At runtime the REAL
 * frozen 1.5.2 EntityMinecartHopper (remapFcCode include) wins — this
 * stub is jar-excluded and dev-deleted like the other minecart classes.
 */
public class EntityMinecartHopper extends EntityMinecart {
    public EntityMinecartHopper(World world) { super(world); }
}
