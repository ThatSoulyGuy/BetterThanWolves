package btw.modern;

/**
 * Cave generation feature. Extends MapGenBase.
 * Mirrors net.minecraft.src.MapGenCaves.
 *
 * Cave generation is complex and not needed for initial gameplay;
 * this stub ensures calls don't crash.
 */
public class MapGenCaves extends MapGenBase {

    @Override
    public void generate(IChunkProvider provider, World world, int chunkX, int chunkZ, byte[] blockArray) {
        // Cave generation stub - no-op for now
    }
}
